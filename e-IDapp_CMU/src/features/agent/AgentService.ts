import CredoAgentService from '../../services/CredoAgentService';

/**
 * AgentService wrapper
 * Provides access to the Credo agent service instance
 */
export const agentService = new CredoAgentService();

export default agentService;

