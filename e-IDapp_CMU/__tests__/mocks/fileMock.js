/**
 * Mock for Static Assets (Images, Fonts, etc.)
 * 
 * This mock is used to handle static asset imports in tests.
 * Returns an empty string for all asset imports to prevent errors.
 */

module.exports = '';
